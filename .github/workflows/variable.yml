name: Variables
on: [push, workflow_dispatch]
jobs:
    print:
        runs-on: ubuntu-latest
        env:
            USERNAME: ${{ vars.USERNAME}}
        steps:
            - run: date
            - run: echo "${USERNAME}"